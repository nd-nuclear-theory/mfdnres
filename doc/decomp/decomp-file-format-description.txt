# Decomposition file format
#
# 08/19/26 (mac): Manually generated from coefs and eigenvalues files.
# 09/06/26 (mac): Revise to reflect actual division of Labels and Eigenvalues and add Lanczos.
# 09/12/26 (mac): Update to reflect actual output from new decomposition I/O code.

################################################################
# Initial comments
################################################################

# The annealar may still provide initial comments.  These are not meant to be
# parsed.

# Z=1 N=1
# decomposition type: U3S
# coefficients: [6.52978799e+01 2.38942345e+00 1.00029689e-02]
# iterations: 1000, goodness: 0.9999999950014838, message: ['Maximum number of iteration reached']

################################################################
# Coefficients
################################################################

# Operators are defined in mcscript/ncci/decomposition.py:
#
#     - Identity: "identity"
#     - Spatial: "Nex", "CSU3", "CSp3R" 
#     - Angular momentum: "L2", "Sp2", "Sn2", "S2"
#     - Isospin: "T2"
#
# New operators may be added by registering them in ncci.decomposition_basis_operator_registry.
#
# Naming conventions will have to be established for, e.g., operators entering into
# shell model j-shell configuration decompositions.  These include the j-shell
# occupation opeartors from shell/programs/obutils/number-op-gen.cpp and j-shell
# seniority operators from shell/programs/tbutils/quasispin-gen.cpp.  E.g.,
#
#     occupation-<species>-<n>-<l>-<2*j>
#
#     quasispin2-<species>-<n>-<l>-<2*j>
#
#     J2-<species>-<n>-<l>-<2*j>

[Coefficients]
Nex = +6.529788e+01
CSU3 = +2.389423e+00
S2 = +1.000297e-02


################################################################
# Info
################################################################

# It is heplful to the user to add summary statistics, even though these
# could in principle be derived from the table of eigenvalues.  In particular,
# the number of numerically *distinct* eigenvalues informs the largest useful
# number of Lanczos iterations.
#
#   num_entries (int): Number of entries in the table, which is equalt to the number
#   of irrep (or symmetry subspace) tuple labels. 
#
#   num_eigenvalues (int): Number of numerically distinct eigenvalues (at numerical
#   precision, but this should be pretty accurate, if the eigenalue formulas are
#   applied identically across entries.  Or, with a little more coding work, a numerical
#   tolerance could be introduced when deducing this count.

[Statistics]
num_entries = 2
num_eigenvalues = 2


################################################################
# Labels
################################################################

# This is where the label names are first given.  These are serve as the "column
# headers" for the eigenvalue table in the following section.
#
# For existing decomposition types, follow naming scheme in
# mfdnres/decomposition.py.  E.g., for U3S, the label names are found is the
# following code:
#
#     U3SLabels = collections.namedtuple("U3SLabels", ["N_omega", "lambda_omega", "mu_omega", "S"])
#
# Naming conventions will have to be established for shell model j-shell configuration decomposition
# labels, e.g.,
#
#     occupation-<species>-<n>-<l>-<2*j>
#
#     seniority-<species>-<n>-<l>-<2*j>
#
#     J-<species>-<n>-<l>-<2*j>

[Labels]
N_omega
lambda_omega
mu_omega
S


################################################################
# Eigenvalues
################################################################

# In each row, the values for the labels are followed by the corresponding eigenvalue.
#
# The specification of labels does not declare which ones are expected to be
# integer or which are expected to be half integer.  The parsing code should
# cast all *numerically* integer label values back to true integers on input.

[Eigenvalues]
     0      0      0      0    +0.000000e+00
     0      0      0      1    +2.000594e-02


################################################################
# Lanczos
################################################################

# *After* a decomposition run, the run scripting appends a table of the Lanczos
# alpha and beta coefficients, as output by MFDn.

[Lanczos]
+2.348112e+01 +7.762340e+00
+4.620439e+01 +1.740142e+01
+2.717671e+01 +1.625244e+01
...
